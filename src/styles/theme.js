const interpark = {
  keyColor: "#d40000",
  hoverColor: "#b50000",
  subColor: "#00ff50"
};

const melonticket = {
  keyColor: "#00ff00",
  hoverColor: "#00b500",
  subColor: "#00ff0050"
};

const ticketlink = {
  keyColor: "#00ff00",
  hoverColor: "#00b500",
  subColor: "#00ff0050"
};

const yes24 = {
  keyColor: "#00a2ff",
  hoverColor: "#005bb5",
  subColor: "#00a2ff50"
};

const practice = {};

const theme = {
  interpark,
  melonticket,
  ticketlink,
  yes24,
  practice
};

export default theme;
